# Clinker 安装

推荐使用`conda`：
```bash
conda install -c conda-forge clinker
```

也可以使用`pip`：
```bash
pip install clinker
```

# 使用
> [!bug] 切勿一**次性扔入过多基因组，否则可能导致内存不足崩溃**。建议分批处理。
> 推荐输入 `30-50` 个基因组。其中 `1/3` 来自感兴趣的基因所在的`clade`。