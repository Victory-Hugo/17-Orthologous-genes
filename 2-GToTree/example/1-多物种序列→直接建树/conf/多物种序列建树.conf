SCG_SET="Bacteria"    # Domain for SCG set
PARALLEL_JOBS="16"    # GToTree -j
HMM_CPUS="16"         # GToTree -n
MUSCLE_THREADS="16"   # GToTree -M

# -------- Paths --------
PROJECT_ROOT="/mnt/l/19-Rv1819c-Gene/5-物种树"
DATA_DIR="${PROJECT_ROOT}/data"       # Input data directory
OUTPUT_DIR="${PROJECT_ROOT}/output"   # Output root
LOG_DIR="${OUTPUT_DIR}/logs"          # Logs
CONF_DIR="${PROJECT_ROOT}/conf"       # Config directory
FASTA_LIST="/mnt/l/19-Rv1819c-Gene/5-物种树/conf/3-物种树抽样8345.file_path.tsv" # File list of fasta paths
# MAP_FILE="${CONF_DIR}/genome_to_id_map.tsv" # Genome name to ID map (optional)

# -------- Run options --------
TREE_METHOD="FastTree"    # "FastTree" or "IQ-TREE"
KEEP_ALIGN="true"         # Keep per-gene alignments (-k)

# -------- Run identifiers --------
RUN_ID_PREFIX="run"
RUN_ID_TIME_FORMAT="%Y%m%d_%H%M%S"
LOG_TIME_FORMAT="%Y-%m-%d %H:%M:%S"
LOG_FILE="${LOG_DIR}/GToTree.log"
