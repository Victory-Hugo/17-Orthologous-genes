我需要你用 **Bash + NCBI BLAST+** 编写一个 **可复现、可批量运行的 Reciprocal Best Hit（RBH）流程**，用于蛋白序列的正交同源筛选。
所有代码放在`/mnt/f/onedrive/文档（科研）/脚本/Download/17-Orthologous-genes/7-RBH/script`目录下。
所有的输出文件全部放在`/mnt/l/19-Rv1819c-Gene/2-RBH/output/`
**已知输入文件如下：**

1. 参考基因组蛋白组（用于反向 BLAST）：
   `/mnt/f/onedrive/文档（科研）/脚本/Download/17-Orthologous-genes/7-RBH/conf/GCF_000195955.2.faa`
2. 参考锚点蛋白（Rv1819c）：
   `/mnt/f/onedrive/文档（科研）/脚本/Download/17-Orthologous-genes/7-RBH/conf/Rv1819c.faa`
3. 候选蛋白集合目录（每个文件是一个基因组的候选蛋白 FASTA）：
   `/mnt/l/19-Rv1819c-Gene/1-BLAST/data/search/sequence/`
   （目录中包含大量 `.faa` 文件）

**任务目标：**
对目录中每一个 `.faa` 文件，执行以下 RBH 判定流程：

**Step 1（正向 BLAST）**

* 使用 `blastp`
* Query：`Rv1819c.faa`
* Database：当前 `.faa` 文件
* 找到 **best hit（按 bit score 或 e-value 排序）**

**Step 2（反向 BLAST）**

* 使用 `blastp`
* Query：Step 1 得到的 best hit 蛋白序列
* Database：`GCF_000195955.2.faa`（完整参考蛋白组）

**RBH 判定标准：**

* 正向 BLAST 的 best hit 存在
* 反向 BLAST 的 best hit 为 **Rv1819c**
* 双向 BLAST 均满足：

  * `e-value ≤ 1e-10`
  * query coverage ≥ 70%
  * subject coverage ≥ 70%

**实现要求：**

* 使用 `makeblastdb` 预先为所有数据库建库
* 使用 `blastp -outfmt 6`（包含：qseqid sseqid pident length qlen slen qstart qend sstart send evalue bitscore）
* 自动计算 query/subject coverage
* 支持批量遍历目录中的所有 `.faa` 文件
* 将每个 `.faa` 的 RBH 结果写入一个汇总 TSV 文件，字段至少包括：

  * genome_faa
  * forward_best_hit
  * forward_evalue
  * reverse_best_hit
  * reverse_evalue
  * forward_qcov
  * reverse_qcov
  * RBH_status（TRUE/FALSE）

**其他要求：**

* 不使用任何正交同源预测软件（如 OrthoFinder、eggNOG 等）
* 不进行结构域分析，仅基于 BLAST
* 代码应具有清晰注释，适合科研复现
* 假设运行环境已安装 BLAST+

请直接输出 **完整可运行的 Bash 脚本**，不要省略步骤，不要写伪代码。

